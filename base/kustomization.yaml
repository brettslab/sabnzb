apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap-gluetun.yaml
  - externalsecret-gluetun-windscribe.yaml
  - externalsecret-news-server.yaml
  - pvc.yaml
  - pvc-incomplete.yaml
  - deployment.yaml
  - service.yaml

labels:
  - includeSelectors: true
    pairs:
      app: sabnzbd
